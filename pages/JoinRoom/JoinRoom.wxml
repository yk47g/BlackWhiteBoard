<view class="main">
    <text class="joinText" >加入</text>
    <form bindsubmit="" class="formClass">
        <input type="text" value="" placeholder="输入房间ID" />
        <button class="btn" type="primary" loading="{{loading}}" disabled="{{disabled}}" bindtap="onTap">
            提交
        </button>
    </form>
</view>